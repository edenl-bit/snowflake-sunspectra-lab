# Snowflake connection (use your own account)
# Copy this file to .env and fill in your values.
# .env is gitignored—your .env stays local and is NEVER committed to the repo.
# Put your programmatic access token (PAT) in SNOWFLAKE_PASSWORD—not your account login password.

SNOWFLAKE_ACCOUNT=your_account
SNOWFLAKE_USER=your_user
SNOWFLAKE_PASSWORD=your_programmatic_access_token
SNOWFLAKE_WAREHOUSE=your_warehouse
SNOWFLAKE_DATABASE=your_database
SNOWFLAKE_SCHEMA=your_schema
SNOWFLAKE_ROLE=your_role

# Optional: for export_snowflake_to_csv.py (source database/schema to export from)
# If not set, export script uses SNOWFLAKE_DATABASE and SNOWFLAKE_SCHEMA
# SNOWFLAKE_EXPORT_DATABASE=your_export_database
# SNOWFLAKE_EXPORT_SCHEMA=your_export_schema

# Export ALL schemas (509 tables across 15 schemas) into data/SCHEMA_NAME/TABLE.csv
# Set to 1 then run: python export_snowflake_to_csv.py
# SNOWFLAKE_EXPORT_ALL_SCHEMAS=1
